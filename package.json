{
  "name": "gmail-reply-extension",
  "version": "1.3.2",
  "description": "A review-first Gmail reply assistant that calls Gemini directly with the user's API key.",
  "scripts": {
    "check": "node --check src/background.js && node --check content/content.js && node --check popup/popup.js",
    "test": "node --test tests/*.test.js && npm run check"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
